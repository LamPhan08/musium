import posed from "react-native-pose";

export const Scaler = posed.View({
    active: {scale: 1.4},
    inactive: {scale: 1},
})

export const ZoomScaler = posed.View({
    active: {scale: 1.2},
    inactive: {scale: 1},
})


